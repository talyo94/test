import { ImgSrcCheckDirective } from './img-src-check.directive';

describe('ImgSrcCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgSrcCheckDirective();
    expect(directive).toBeTruthy();
  });
});
